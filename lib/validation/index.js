// see http://validatejs.org/ for documentation on how to do contraints
var common = require("mykoop-utils/common");
var validate = common.validate;

// just so it compiles until a real validation method is created
exports.t = 0;
